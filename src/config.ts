
// Max file size for files in MegaBytes
export const maxFileSize = 2000;
export const port = "8000";
export const backendURL = `http://127.0.0.1:${port}`;
export const persistence = false; // If persistence is false input folder is deleted on app closing.
